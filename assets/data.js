export default [
  {
    type: 'String Method',
    title: 'String.split()',
    implementation: `It's a method available on any variable that holds a string as it's value`,
    arguments: `Accepts a string with which to delimit the string on which it is called`,
    sample: `const str = split.this.string.up`
  }
]

/*** string ***/
// slice
// splice

/*** array ***/
// join
// concat
// filter
// sort

/*** object ***/
// how to deep merge